import TeamFlag from './TeamFlag.vue';
import MatchResult from './MatchResult.vue';
import DateTime from './DateTime.vue';

/**
 * This file exports three formatters:
 * - TeamFlag: A component that displays a team's flag and name.
 * - MatchResult: A component that displays the result of a match.
 * - DateTime: A component that displays a date and time.
 */
export default {
  TeamFlag,
  MatchResult,
  DateTime,
};
